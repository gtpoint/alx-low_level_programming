#include "main.h"
#include <string.h>
/**
 * _strlen - this function that returns the length of a string.
 * @s: the parameter
 * Return: lenght of the paraleter be printed
 */
int _strlen(const char *s)
{
	return (strlen(s));
}
