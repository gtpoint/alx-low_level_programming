#include "main.h"
/**
 * add - this is a function that adds two integers and returns the result
 * @a: the number one
 * @b: the number two
 *
 * Return: the result of addition
 */
int add(int a, int b)
{
	return (a + b);
}
